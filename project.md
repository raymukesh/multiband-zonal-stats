# Project Context: Fast Multiband Zonal Statistics

This document is the durable technical handoff for continuing the project on a
different computer or in a new development session. Read it together with
`README.md` and `CHANGELOG.md`.

## Mission

Build a fast, dependable QGIS plugin that calculates zonal statistics for many
polygon or multipolygon features across multiband rasters and writes clean,
predictable CSV output. Performance is the primary design constraint.

The plugin must avoid the common slow pattern of reading the raster separately
for every polygon and every band. It should stay responsive, use bounded memory,
handle overlapping zones correctly, preserve useful names, and support pixel and
area-based reporting.

## Current status

- Current release: `0.1.1`
- Repository default branch: `main`
- QGIS metadata minimum version: QGIS 3.34
- Confirmed runtime: QGIS 4.2.0 on macOS with Python 3.12, Qt 6, GDAL 3.12
- Distribution: installable ZIP generated by `scripts/build_package.py`
- License: MIT
- Primary algorithm ID: `fastzonalstats:multiband_zonal_statistics`

Version 0.1.1 has been tested by constructing the dialog in the QGIS 4.2
runtime and by running the complete algorithm on a generated two-band raster
with overlapping polygons. The smoke test produced the expected four CSV rows.

## Current user workflow

The user can run the plugin from:

- the Raster menu and plugin toolbar;
- the custom graphical dialog;
- the Processing Toolbox;
- QGIS Model Builder or another Processing client.

The graphical dialog provides:

- raster and polygon layer selectors;
- polygon ID and name field selectors;
- live validation of band expressions such as `all`, `1-8`, or `1,3,7`;
- selectable statistics;
- pixel and projected-area units;
- output file selection;
- tile size, band chunk size, and all-touched advanced options;
- background progress, safe cancellation, and result opening.

## Implemented statistics and output

The engine currently calculates these continuous-raster statistics per polygon
and band:

- valid pixel count;
- nodata pixel count;
- minimum and maximum;
- sum and mean;
- population standard deviation.

The default CSV is long format with one row per polygon and selected band:

```text
polygon_id,polygon_name,band_index,band_name,area,area_unit,count,nodata,min,max,sum,mean,stddev
```

Raster band descriptions are used when present. Missing descriptions become
stable names such as `band_007`. CSV output is UTF-8 and is written to a
temporary file before an atomic rename, so cancellation or failure does not
publish a partial result.

Supported area units are pixels, square metres, square kilometres, hectares,
and acres. Metric areas are based on the raster affine-transform determinant and
the projected CRS linear-unit conversion.

## Performance architecture

The raster, not each polygon, is the unit of work:

1. The Processing algorithm validates inputs and copies each feature geometry.
2. Geometries are transformed once into the raster CRS.
3. An affine-aware tile index maps zone bounds to relevant raster tiles in
   approximately O(number of zones), avoiding a full polygon scan per tile.
4. The raster is read in bounded windows and selected bands are read in chunks.
5. Candidate polygons are rasterized into an in-memory integer label grid.
6. NumPy `bincount`, `minimum.at`, and `maximum.at` operations update zone
   accumulators without Python loops over raster pixels.
7. Finished band chunks are streamed to the CSV in stable band/zone order.

Peak memory is primarily controlled by `tile_size` and `band_chunk_size`. The
current defaults are 1024 pixels and eight bands.

### Multipart and overlapping zones

All parts of a multipart feature share one internal zone ID. Holes are retained.

A label raster can hold only one zone ID per pixel, so overlapping candidates
are greedily divided into passes whose bounding boxes do not overlap. Each pass
gets a separate label mask. This is conservative—it may create an unnecessary
extra pass when bounding boxes overlap but geometries do not—but it preserves
correct results for overlapping polygons. Touching bounds are also separated so
the all-touched option cannot lose a boundary pixel to label overwriting.

## Source layout

| Path | Responsibility |
| --- | --- |
| `__init__.py` | QGIS `classFactory` entry point |
| `plugin.py` | Provider registration, menu action, toolbar action, dialog lifecycle |
| `provider.py` | Processing provider and algorithm registration |
| `algorithm.py` | QGIS parameters, feature preparation, CRS transformation, feedback |
| `engine.py` | QGIS-independent GDAL/NumPy tile engine and atomic CSV writer |
| `dialog.py` | Graphical workflow and background `QgsProcessingAlgRunnerTask` |
| `utils.py` | Dependency-free band parsing, naming, statistics, and path helpers |
| `metadata.txt` | QGIS Plugin Manager metadata and version |
| `icon.svg` | Plugin and toolbar icon |
| `tests/test_utils.py` | Pure-Python parsing and naming tests |
| `tests/test_engine_helpers.py` | Tile, overlap-pass, and area helper tests |
| `tests/qgis_runtime_smoke.py` | Generated-data end-to-end QGIS runtime test |
| `scripts/build_package.py` | Reproducible installable ZIP builder |

The repository root is the plugin source root. The packaging script places these
files under the required `fast_multiband_zonal_stats/` directory inside the ZIP.

## Compatibility decisions

- The Processing provider is registered even when QGIS loads the plugin without
  a desktop interface. GUI imports and actions are deferred for headless use.
- The algorithm provides its own translation helper because QGIS 4 no longer
  exposes the inherited `tr()` behavior used by older examples.
- Dialog enum access supports both PyQt5 and PyQt6 enum scoping.
- The engine uses the GDAL/NumPy installations bundled with QGIS; the plugin has
  no separately downloaded runtime dependencies.
- Each background run receives copied geometry data and paths instead of using
  GUI widgets from a worker thread.

## Validation

Run the dependency-free tests from the repository root:

```bash
python3 -m unittest discover -s tests -v
python3 -m compileall -q .
git diff --check
```

The current suite contains 12 unit tests. The QGIS runtime smoke test requires
QGIS's bundled Python environment and is intentionally separate from unit-test
discovery.

Before a release, manually verify at least:

- a multiband raster with and without band descriptions;
- polygon and multipolygon layers;
- overlapping zones and touching boundaries;
- holes, empty geometries, nodata, and zones outside the raster;
- a CRS transformation between the vector and raster;
- pixel-centre and all-touched modes;
- cancellation without a partial final CSV;
- square-metre and square-kilometre output in a projected CRS.

Build the installable package with:

```bash
python3 scripts/build_package.py
```

Update both `metadata.txt` and the filename in `scripts/build_package.py` when
bumping the version, and add the change to `CHANGELOG.md`.

## Known limitations

- Metric area output currently requires a projected raster CRS. Geographic
  rasters must use pixels or be reprojected to an appropriate equal-area CRS.
- Categorical class counts, class areas, and percentages are not implemented.
- Exact median and percentile statistics are not implemented because they need
  a different, more memory-intensive aggregation strategy.
- The first release uses one optimized background processing task; adaptive
  multi-worker execution has not yet been benchmarked or implemented.
- Invalid non-empty geometries are not automatically repaired.
- The fast engine is designed primarily for GDAL-readable raster sources. Remote
  services and unusual provider-only QGIS raster sources need explicit testing.
- There is no benchmark report yet against QGIS's standard zonal-statistics
  workflow on representative large datasets.

## Recommended roadmap

1. **Benchmark before further optimization.** Add repeatable synthetic and real
   dataset benchmarks measuring elapsed time, peak memory, raster tiles read,
   polygon count, and band count.
2. **Categorical mode.** Produce one row per polygon, band, and raster class with
   pixel count, area, and percentage, plus an optional class-name lookup table.
3. **Geographic area handling.** Add geodesic pixel areas or a clearly managed
   equal-area working CRS without silently treating degrees as metres.
4. **Adaptive memory planning.** Replace the fixed band-chunk default with a
   memory budget that considers tile dimensions, raster dtype, band count, and
   accumulator size.
5. **Geometry diagnostics.** Add optional repair, per-feature status, and clearer
   reporting for empty, invalid, outside-raster, and no-valid-pixel zones.
6. **Optional statistics.** Add variance, approximate percentiles, and exact
   percentiles only when explicitly requested.
7. **Release automation.** Add CI for unit tests, QGIS 3/4 compatibility checks,
   package validation, and tagged ZIP releases.

## Non-negotiable design principles

- Never introduce a polygon × band loop that repeatedly rereads full raster
  areas.
- Never perform per-pixel calculations in Python when a GDAL or NumPy operation
  can do the same work.
- Preserve correctness for overlapping polygons, multipart features, holes, and
  per-band nodata.
- Keep memory bounded and make costly accuracy options explicit.
- Keep Processing and graphical runs on the same algorithm implementation.
- Keep output column names, row ordering, band names, and encoding deterministic.
- Do not publish a partially written CSV after cancellation or failure.

